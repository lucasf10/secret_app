import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  container: {
    width: '100%',
    height: 55,
    borderWidth: 1,
    borderColor: '#ccc',
    paddingLeft: 30,
    color: '#000000',
  },
});

export default style;
