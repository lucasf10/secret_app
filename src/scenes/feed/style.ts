import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    view: {
        flex: 1,
        marginBottom: -4,
    },
    loader: {
        padding: 10,
    },
});

export default style;
